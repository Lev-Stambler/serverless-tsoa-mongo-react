import { User } from "./UserSchema";

/**
 * Future considerations include having a "parent/guardian" user type and the family would include a list of them
 * Also include adding number of tickets 
 */
export interface Family {
  contactEmail: string,
}
